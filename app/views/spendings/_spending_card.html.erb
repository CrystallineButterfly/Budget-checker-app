<div class="container">
  <table class="table table-striped", style="text-align: center;">
    <thead>
      <tr>
        <th>No.</th>
        <th>Name</th>
        <th>Amount</th>
      </tr>
    </thead>
    <tbody>
      <% @catergory.spendings.each_with_index do |spending, index| %>
        <tr>
          <td><%= index + 1 %></td>
          <td><%= spending.name %></td>
          <td>$ <%= spending.amount %></td>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
    </div>
</div>